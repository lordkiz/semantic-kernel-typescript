/**
 * Represents the content of a chat message
 */
export enum ChatMessageContentType {
  /**
   * The content is text
   */
  TEXT = "TEXT",
  /**
   * The content is an image
   */
  IMAGE_URL = "IMAGE_URL",
}
